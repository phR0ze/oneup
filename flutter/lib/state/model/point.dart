class Point {
  final int id;         // Unique identifier for the user
  final int value;      // User name that can be changed
  final int userId;     // User ID to which the point belongs

  Point(this.id, this.value, this.userId);
}